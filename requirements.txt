# Python 3 dependencies.
#
# `pynmrstar` parses the NMR-STAR entry files; its parser is a C extension, so
# there is nothing else to build.  It replaced the BMRB `starobj`/`sas` pair,
# which are no longer needed at runtime -- only by tests/golden_*.sh, which
# runs the legacy Python 2 loader to produce the comparison golden.
#
psycopg2-binary
pynmrstar
